name: autopilot-pr-triage-scheduler
version: 0.1.0
description: Queue open community pull requests and fan out isolated PR-triage worker slots (default 2). Advisory only. Never merges, never assigns, never requests reviewers.
author: Microsoft
license: MIT

dependencies:
  apm:
    - ../autopilot-pr-triage-worker
  mcp: []

scripts: {}
