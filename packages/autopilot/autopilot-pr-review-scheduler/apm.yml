name: autopilot-pr-review-scheduler
version: 0.1.0
description: Queue panel-review pull requests (accepted only) and fan out isolated advisory review slots (default 2). Never implements. Never composes autopilot-pr-merge-worker.
author: Microsoft
license: MIT

dependencies:
  apm:
    - ../autopilot-pr-review-worker
  mcp: []

scripts: {}
